# =============================================================================
# DEPENDENCY-SCANNING SHIM — NOT the install source of truth.
#
# This file exists SOLELY so GitLab Dependency Scanning spawns for the
# backend (issue #18): the DS template's `rules:exists` patterns key on
# requirements.txt / Pipfile.lock / poetry.lock / uv.lock / setup.py —
# a PEP 621 pyproject.toml alone matches no pattern, so no analyzer job
# ever spawned.
#
# The install source of truth is backend/pyproject.toml (CI installs via
# `pip install -e ".[dev]"`). This file is a TEXTUAL mirror of the
# dependency specifiers declared there and MUST be regenerated whenever
# [project].dependencies or [project.optional-dependencies].dev change
# in pyproject.toml — drift here silently narrows scan coverage.
# =============================================================================

# --- [project] dependencies (runtime) ---
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
alembic>=1.13
psycopg[binary]>=3.2
pydantic>=2.8
pydantic-settings>=2.4
redis>=5.0
pyjwt>=2.9

# --- [project.optional-dependencies] dev (CI toolchain) ---
pytest>=8.3
pytest-cov>=5.0
httpx>=0.27
ruff>=0.6
mypy>=1.11
import-linter>=2.0
hypothesis>=6.100
